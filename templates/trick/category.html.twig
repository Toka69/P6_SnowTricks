{%  extends "base.html.twig" %}

{% block title %}
    Catégorie: {{ slug }}
{% endblock %}

{% block body %}
    <h1>Catégorie: {{ category.name }}</h1>
    {% for trick in category.tricks %}
        <p>{{ trick.name }}</p>
    {% endfor %}
{% endblock %}